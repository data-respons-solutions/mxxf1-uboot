if TARGET_CARGOTEC

config SYS_BOARD
	default "cargotec"

config SYS_VENDOR
	default "datarespons"


config SYS_CONFIG_NAME
	default "cargotec"

config PMIC_I2C_BUS
	int
	default 0

config FACTORY_BOOT
	bool "Factory boot mode"
	default n
	help
	  Enables the factory installation boot mode

config LOGLEVEL
	int "Kernel loglevel"
	default 4
	
config MXC_USB_OTG_OC_LOW
	bool "OTG overcurrent active low"
	default y

endif
